spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://120.79.185.138:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
